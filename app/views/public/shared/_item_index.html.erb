<% items.each do |item| %>
  <div>
    <%= link_to item_path(item.id) do %>
      <%= attachment_image_tag item, :image, format: 'jpeg', fallback: "no_image.jpg", size: "220x150" %>
    <% end %>
  </div>
  <div><%= item.name %></div>
  <div>￥<%= item.with_tax_price.to_s(:delimited) %></div>
<% end %>