<div class="container">
  <div class="row">
    <div></div>
    <div><%= %></div>
    <div>
      <h2><%= @item.name %></h2>
      <div><%= @item.introduction %></div>
      <div>￥<%= (@item.price*1.1).floor.to_s(:delimited) %>(税込)</div>
    </div>
  </div>
</div>