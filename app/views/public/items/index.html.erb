<div class="container">
  <div class="row">
    <div class="col-3">
      <div>ジャンル検索</div>
      <% @genres.each do |genre| %>
        <div><%= genre.name %></div>
      <% end %>
    </div>
    <div class=col-9">
      <h2>商品一覧</h2>
      <div></div>
      <%= paginate @items %>
    </div>
  </div>
</div>