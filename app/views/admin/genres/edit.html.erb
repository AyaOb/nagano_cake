<h2>ジャンル編集</h2>
<%= form_with model: @genre, url: admin_genre_path(@genre.id), local: true do |f| %>
  <div class="form-group">
    <%= f.label :name, "ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit "変更を保存" %>
  </div>
<% end %>